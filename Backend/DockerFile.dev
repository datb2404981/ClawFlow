# Chỉ cài phụ thuộc; mã nguồn + watch: `docker compose` service `backend` (docker-compose.yml)
FROM node:20-alpine

WORKDIR /app

COPY package*.json ./
RUN npm install

EXPOSE 8080

# compose.dev ghi đè: npm run start:dev
CMD ["npm", "run", "start:dev"]
